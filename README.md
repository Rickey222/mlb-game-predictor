# Data

This folder stores MLB data used by the project.

## Folders

- `raw/` contains original downloaded data.
- `processed/` contains cleaned datasets ready for modeling.

Raw and processed data files are ignored by Git so large datasets are not pushed to GitHub.